{
  "builds": [
    { "src": "client/package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "https://estate-management-system-ccbt.onrender.com/api/$1" },
    { "src": "/(.*)", "dest": "/client/dist/$1" }
  ]
}
